<UserControl x:Class="PolygonEditor.AppControl.ColorEdit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="120">
    <ComboBox VerticalAlignment="Stretch" HorizontalAlignment="Stretch" 
              ItemsSource="{Binding Path=ColorItemsCollection}" 
              SelectedItem="{Binding SelectedItem}" Background="{Binding ColorValue,Mode=OneWay}">
    </ComboBox>
</UserControl>